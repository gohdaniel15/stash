<%= form_for @goal, url: goals_path, remote: true do |f| %>
  <%= f.text_field :content, placeholder: "Enter your goal"  %>
  <%= f.select :size, ["Big", "Medium", "Small"] %>
  <%= f.submit %>
<% end %>
